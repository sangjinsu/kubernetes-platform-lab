apiVersion: v1
kind: Service
metadata:
  name: lab-echo
  namespace: lab-chapter-01
  labels:
    app.kubernetes.io/name: lab-echo
    app.kubernetes.io/part-of: chapter-01
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: lab-echo
  ports:
    - name: http
      port: 80
      targetPort: http
